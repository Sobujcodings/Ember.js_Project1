import Component from '@glimmer/component';
// import { tracked } from '@glimmer/tracking';
// import { action } from '@ember/object';
// import { tracked } from '@glimmer/tracking';

export default class SearchbarComponent extends Component {
    // @tracked query = '';

}

// on click korar por ai handle or input neche

// So, when you see this.inputValue, it means you are accessing or modifying the inputValue property of the component instance.

// Now that we have our component all set up, we can finally wire up our search box and store our search query! First things first: let's create a component class(searchbar.js) to store our query state.

// Next, we'll wire up our query state in the component template.